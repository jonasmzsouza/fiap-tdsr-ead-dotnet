@{
    ViewData["Title"] = "Home Page";
}

<!-- Implementar o CRUD para o Model
        Livro: (Codigo, Titulo, Data Lançamento, Valor, Categoria (Enum), Novo (bool), Editora (String enviada pelo controller))

    Criar a Model
    Criar o Controller

    Cadastar:
        Criar a action (Método) GET: abrir a página com o formulário
        Criar a View com o formulário HTML (partial view para o fomulário)
        Criar a action POST: recebe os dados, adiciona na lista, seta a mensagem e faz um redirect

    Listar:
        Criar/Ajustar a action GET: envia a lista para a view
        Criar a View com a tabela HTL para exibir os dados

    Editar:
        Ajustar a view de listagem para adicionar o link que envia o Id que será editado
        Criar a action GET: recebe o id, pesquisa o model e envia para a view
        Criar a View com o fomulário HTML (partial view para o formulário)
        Criar a action POST: recebe os dados, atualiza na lista, seta a mensagem e faz um redirect

    Remover:
        Ajustar a view de listagem para adicionar o modal que possui o formulário de remoção que envia o Id
        Criar a action POST: recebe o id, remove da lista, seta a mensagem e faz um redirect

    Criar um tag helper
    Ajustar o Layout, mudar o tema

-->
